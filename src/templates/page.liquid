<div class="content_area">
  <div class="container">
    <h1 class="display-4 text_width mx-auto mt-5 pt-5">{{ page.title }}</h1>
    <div class="row">
      <div class="col">
      {{ page.content }}
      </div>
    </div>
  </div>
</div>
